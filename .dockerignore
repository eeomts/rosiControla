# O docker NAO le o .gitignore -- sem este arquivo, o COPY levaria o
# config.ini local (com as credenciais da maquina) para dentro da imagem.

# segredos: nunca dentro da imagem
.env
config/config.ini
deploy/config.ini
deploy/dev/config.ini

# dados e artefatos do host
data/
vendor/
.phpunit.cache/
*.log
storage/logs/

# nao servem em producao
tests/
phpunit.xml
router-dev.php
TODO/
app/
.git/
.claude/
